"use client";

/**
 * Placeholder for Command Snippets list/form (Task 9).
 * Renders a simple placeholder so the page loads.
 */
export function CommandSnippetsClient() {
  return (
    <div className="rounded-lg border border-dashed p-6 text-muted-foreground">
      Command snippets
    </div>
  );
}
